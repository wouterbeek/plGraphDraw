% Index for the plGraphDraw library.
% File search path plGraphDraw must exist before this is loaded.

user:file_search_path(plGraphDraw_fca, plGraphDraw(fca)).
user:file_search_path(plGraphDraw_graph, plGraphDraw(graph)).
user:file_search_path(plGraphDraw_tree, plGraphDraw(tree)).

